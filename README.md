# Task Manager (Console)

Console task manager written in C++. Tasks are saved to `tasks.txt` and persist between sessions.

## Features
- Add task
- Show all tasks
- Delete task by number
- Auto-save to file

## Compile
```bash
g++ main.cpp -o taskmanager
```
Run
```bash
./taskmanager
